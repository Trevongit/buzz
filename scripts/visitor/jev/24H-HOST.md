# Jev doorman on a 24h host (prep)

Not tonight. No TypeSafe key in this file. No hostnames.

When the laptop sleeps, the doorman sleeps. Later extras + home-grok can run the **same caller** on the always-on community machine.

## What travels

- Desktop experiment `jevDoorman` (Settings → Experiments). Default **off**.
- extras inbox: `VISITOR_JEV=1`
- questions: `scripts/visitor/jev/THREE-JOBS.json`
- lamp file: `~/.buzz-dev/control-room/jev-last.json`
- audit: `~/.buzz-dev/control-room/jev-audit.jsonl`

## What stays on the machine (never git)

- TypeSafe key in a local env file (today: JEV-research `.env` on the laptop)
- On the 24h host: a **new** `jev.env` on that box, same variable name, not copied into Buzz Agents

## Do not

- Put the key on the Agents page
- Open the usage page to the whole LAN without a lock
- Give visitor chairs the key
- Mint a Jev person
